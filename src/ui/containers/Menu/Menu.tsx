import React from "react";
import "./Menu.scss";

export default function Menu() {
	return (
        <section className="menu">
            <h1 className="menu__title">This is the Menu screen</h1>
        </section>
	);
}
